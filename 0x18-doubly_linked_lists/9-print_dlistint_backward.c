#include "lists.h"

/**
* print_dlistint_backward - prints all the elements of a list backwards
* @h: head
*
* Return: the number of nodes
*/

size_t print_dlistint_backward(const dlistint_t *h)
{
	(void) h;

	return (EXIT_FAILURE);
}
