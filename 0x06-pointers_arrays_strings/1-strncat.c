#include "holberton.h"

/**
* _strcat - concatenates two strings
* 
*/

char *_strcat(char *dest, char *src)
{
	int j;

	for (j = 0; j != '\0'; j++)
	{
	}
	for (; j != '\0'; j++)
	{
		dest[j] = src[j]
	}
	_putchar('\0');

	return (dest);
}
