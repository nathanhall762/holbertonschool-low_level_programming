# include <stdio.h>
# include <string.h>

/*Sends text to standard output.*/

int main(void)
{
	char str1[54];

	strcpy(str1, "\"Programming is like building a multilingual puzzle");
	puts(str1);
	return (0);
}

